apply plugin: 'maven'

uploadArchives {
    repositories.mavenDeployer {


        pom.groupId = 'com.peace.hp'
        pom.artifactId = 'viewbind'
        pom.version = '1.0.0'


        repository(url: uri('E:/repo'))

    }
}

//执行 gradlew uploadArchives 发布到本地 （E:/repo)
